name: Build
on:
  workflow_dispatch:
  push:
jobs:
  build:
    name: Build 🔧 & Test 🔧
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
    - uses: actions/checkout@v3
    - name: Set up GCC
      uses: egor-tensin/setup-gcc@v1
      with:
        version: latest
        platform: x64
    - name: Build test/calculate.c
      run: gcc ./include/**.c ./test/calculate.c -o ./calculate.out
    - name: Test ./calculate.out 🔧
      run: ./calculate.out
